'use strict';
var assert = require('assert');

describe('node-upwork-oauth2', function () {
  it('can be imported', function () {
    var api = require('..');
    assert(api !== undefined);
  });
});
